program logs;

uses
  Forms,
  logsMain in 'logsMain.pas' {Form1},
  logsSetting in 'logsSetting.pas' {FormSetting};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TFormSetting, FormSetting);
  Application.Run;
end.
